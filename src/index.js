import { 
    renderVisualization,
    formInputChange,
    formReset
 } from './util/eventUtil';

document.addEventListener("DOMContentLoaded", ()=>{   
    renderVisualization();
    formInputChange();
    formReset();
})

